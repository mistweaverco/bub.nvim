---@module 'bub.utils'
return {
  convert = require("bub.utils.convert"),
  is = require("bub.utils.is"),
  is_not = require("bub.utils.is_not"),
  notification = require("bub.utils.notification"),
  str = require("bub.utils.str"),
  tbl = require("bub.utils.tbl"),
}
