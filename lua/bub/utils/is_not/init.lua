---Type checking functions
---@module 'bub.utils.is_not'
return {
  bool = require("bub.utils.is_not.bool"),
  empty = require("bub.utils.is_not.empty"),
  fun = require("bub.utils.is_not.fun"),
  func = require("bub.utils.is_not.func"),
  number = require("bub.utils.is_not.number"),
  str = require("bub.utils.is_not.str"),
  tbl = require("bub.utils.is_not.tbl"),
}
